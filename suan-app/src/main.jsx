import React from 'react'
import ReactDOM from 'react-dom/client'
import TrialScene from './TrialScene.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TrialScene />
  </React.StrictMode>,
)
